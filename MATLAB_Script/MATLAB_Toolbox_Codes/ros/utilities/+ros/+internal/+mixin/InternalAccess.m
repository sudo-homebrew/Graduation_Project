classdef (Abstract) InternalAccess < handle
%This class is for internal use only. It may be removed in the future.

%InternalAccess Base for classes that need access to internal objects
%   If your class needs access to the internal implementation in other
%   classes, derive it from InternalAccess. For example, classes that
%   need access to the internal node objects in ROS 2 nodes can derive
%   from InternalAccess.

%   Copyright 2019-2020 The MathWorks, Inc.

%#codegen

end
